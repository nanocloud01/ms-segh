apiVersion: v1
kind: Service
metadata:
  name: ms-seg-service
spec:
  type: NodePort
  selector:
    app: ms-seg   # 🔥 conecta con los Pods
  ports:
    - port: 80
      targetPort: 1616
      nodePort: 30016
